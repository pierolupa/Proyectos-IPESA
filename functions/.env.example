# Copia este archivo a .env (no se commitea) para desarrollo local con el
# emulador. En producción, `firebase deploy` toma el archivo `.env` de esta
# misma carpeta si existe (ver README.md).

# ID de la hoja de cálculo "IPESA - Guías" (está en la URL de la hoja:
# https://docs.google.com/spreadsheets/d/ESTE_ES_EL_ID/edit)
SHEET_ID=
